<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta og:description="Markdown Blog Using Node.js, Express, And MongoDB">
    <title>Mark Down Blog</title>
    <link href="styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <style>
        body {
            background-color: rgb(15, 18, 32);
            color: aliceblue;
            /* padding: 1.5%; */
        }
    </style>

</head>

<body>

    <div class="container" style="max-width:750px;">
        <h2 class="mb-4">Blog Articles</h2>
        <a href="/articles/new" class="btn btn-success">New Article</a>

        <div class="mt-2" style="border:4px solid greenyellow;padding:2%;border-radius:1rem">
            <% articles.forEach(article=>{ %>
                <div class="card mt-2 mb-4" style="color:black;padding:.5%">
                    <div class="card-body" style="padding:.5rem 1rem .5rem 0rem">
                        <h4 class="card-title">
                            <%= article.title %>
                        </h4>
                        <div class="card-subtitle text-muted mb-2" style="padding:0rem 0rem 0rem 0rem">
                            <%= article.createdAt.toLocaleDateString() %>
                        </div>
                        <div class="card-text  mb-2" style="padding:.4rem 0rem 0rem 0rem">
                            <%= article.description %>
                        </div>
                        <a href="articles/<%= article.slug %>" class="btn btn-primary">Read More</a>
                        <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>

                <% }) %>

        </div>

    </div>

</body>

</html>